version: '3.7'

services:
  postgres:
    build: postgresql 
    restart: always
    ports:
      - 5432:5432
    volumes:
      - type: bind
        source: ${PWD}/postgresql/postgresql.conf
        target: /var/lib/postgresql/data/postgresql.conf
        volume:
          no-copy: true